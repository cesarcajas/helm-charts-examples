apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dashstream.fullname" . }}-test-connection"
  labels:
    {{- include "dashstream.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dashstream.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
